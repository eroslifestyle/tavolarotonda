# Checkpoint — tavolarotonda-due

**Sessione**: 2026-07-11 · **HEAD**: `49e5619` · **Branch**: `main`
**Tipo**: project-code · **Stato**: completata

---

## Goal

Chiusura 3 task TR: TR-046 (unificazione provider), TR-042 (AQ scores in GUI), TR-047 (streaming colorato).

---

## Progress / TODO Board

### ✅ Done (verificati, evidence-gated)

- [x] **TR-046** — Unificato LLMProvider + AnthropicCompatProvider · commit `49e5619`
  - ProviderKind aggiunge `"anthropic_compat"`
  - LLMProvider riceve `anthropic_compat_base_url` + `anthropic_compat_api_key` + `provider_kind` param
  - `_anthropic_compat()` inline; `AnthropicCompatProvider` deprecato → thin wrapper
  - Verify: `python3 -m py_compile tavolarotonda/providers.py && smoke test OK`

- [x] **TR-042** — AQ scores in api_models + top_5_by_aq · commit `49e5619`
  - `api_models()` ritorna `aq_score` per ogni modello + `top_5_by_aq` rank
  - `get_aq_score()` in `tavolarotonda/config.py`
  - AQ: opus-4.8=8.0, MiniMax-M3=7.5, opus-local=4.69, ollama-auto=2.5

- [x] **TR-047** — Streaming SSE con agent_key + agent_color · commit `49e5619`
  - `_emit_event()` aggiunge `agent_key` + `agent_color` a ogni evento
  - `agent_key` estratto da system prompt via `_extract_agent_key()`
  - `agent_color` lookup via `get_agent_color()` da config.yaml

### ⬜ To-Do (nessuna — tutte completate)

Tutte le task TR chiuse. Prossime sul PG-TOD: TR-044 (GitHub Actions CI), TR-045 (integrazione Obsidian), TR-020 (MCP server), TR-021 (opus-m3-confer wiring).

---

## Failed Approaches

Nessuno in questa sessione.

---

## Do NOT

- Non toccare `.env` / secrets (gitignored)
- Non riscrivere la GUI radicalmente
- Non aggiungere auth/multi-user

---

## Key Decisions

1. **Provider unificato**: `AnthropicCompatProvider` deprecato, logica migrata in `LLMProvider._anthropic_compat()`. Nuovo uso: `LLMProvider(anthropic_compat_base_url=..., anthropic_compat_api_key=...)` + `complete(..., provider_kind="anthropic_compat")`.
2. **Retrocompatibilità**: `AnthropicCompatProvider(base_url, api_key)` ancora funziona — thin wrapper.

---

## Current State

TavolaRotonda 2.0 · HEAD `49e5619` · 3 task completate · config.yaml operativo · pushato.

---

## File Changes

| File | Delta |
|---|---|
| `tavolarotonda/providers.py` | +77 / -85 linee (refactoring provider) |
| `gui/app.py` | +17 linee (aq_score + top_5_by_aq + agent_color stream) |
| `tavolarotonda/config.py` | +6 linee (get_aq_score) |
| Vault: `sessioni/2026-07-11_TR-046-042-047.md` | creato |
| Vault: `decisioni/2026-07-11_provider-unification.md` | creato |

---

## Code Context

```python
# LLMProvider ora accetta provider_kind forzato:
provider = LLMProvider(anthropic_compat_base_url="http://x:8787", anthropic_compat_api_key="k")
result = await provider.complete(prompt, model="claude-opus-4-8", provider_kind="anthropic_compat")

# api_models risponde con:
# {
#   "models": [..., "aq_score": 8.0],
#   "top_5_by_aq": [{"key": "opus-4.8", "aq_score": 8.0}, ...]
# }

# SSE event ora include:
# {"agent_key": "aristotle", "agent_color": "#f0883e", ...}
```

---

## Setup

```bash
cd /mnt/backup/Dropbox/1\ Programmazione/Progetti/tavolarotonda-due
# Dipendenze
pip install flask pyyaml  # già in requirements.txt
# Env opzionali
export ANTHROPIC_API_KEY=sk-...
export MiniMax_API_KEY=...
# Avvio
python -m gui.app --port 8912
```

---

## Resume Instructions

Nessun lavoro pendente. Prossimi passi dal PG-TOD:
- TR-044: GitHub Actions CI
- TR-045: integrazione Obsidian (vault topic → output markdown)
- TR-020: MCP server (`tavolarotonda serve --mcp`)

A fine task: committa (conventional) + push + /wiki all.

---

## Warnings

- Nessun warning.

---

## Bootstrap prossima chat — copia-incolla prompt iniziale

```
Leggi il checkpoint: /mnt/backup/Dropbox/1 Programmazione/Progetti/tavolarotonda-due/.claude/checkpoints/CP_20260711_2248.md
Task corrente: tutte le task TR-046/042/047 completate. Prossime: TR-044 (CI), TR-045 (Obsidian), TR-020 (MCP)
Non toccare: .env/secrets, riscrivere GUI, aggiungere auth/multi-user
Quando hai finito, committa con messaggio conventional, pusha, e fai /wiki all.
```
