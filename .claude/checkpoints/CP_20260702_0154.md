# Checkpoint — tavolarotonda-due · 2026-07-02 01:54

## Goal

Rinominare il progetto `tavolarotonda` (ex `/mnt/backup/PROGETTI/`), portarlo in scope canonico Obsidian, e integrare Ornith-35B come provider LLM nativo nel council GUI.

## Progress / TODO Board

### ✅ Done (verificato)

- **TR-001** — `mv /mnt/backup/PROGETTI/tavolarotonda/ → Obsidian/Memoria/progetti/tavolarotonda-due/` · git HEAD `da1f90b` intatto
- **TR-002** — `ornith-35b:latest` integrato in `gui/app.py`: MODELS dict + `_MODEL_FOR_PROVIDER` + routing triade Gruppo C + round-robin alternato · `/api/models` state=ok ✅
- **TR-003** — Fix `_strip_think` in `tavolarotonda/providers.py`: aggiunto `_THINK_BLOCK_RE = re.compile(r"<think>.*?</think>", re.DOTALL)` · test: `capitale Francia = Parigi` latency 0.86s, niente residui ✅
- **TR-004** — Vault: `tavolarotonda-due.md` (entry canonica) + `tavolarotonda.md` (redirect) + `sessioni/2026-07-02-rename-ornith-integration.md` + `PROJECT-TOD.md` bootstrap
- **TR-GUI** — GUI Flask riavviata su `:8912`, Chrome aperto, `/api/council-presets` → ornith-35b in tutti e 3 i preset ✅

### ⬜ To-Do (in ordine)

- [ ] **TR-010** — Commit + push modifiche sessione
      Comando: `cd /home/mrxxx/Obsidian/Memoria/progetti/tavolarotonda-due && git add gui/app.py tavolarotonda/providers.py && git commit -m "feat: integrate ornith-35b as llm provider + fix think-strip" && git push`
      Done when: `git push` exit 0

- [ ] **TR-011** — Test sessione reale Ornith (3 round, mode topic)
      Comando: apri `http://localhost:8912/` → seleziona modello `ornith-35b` → avvia dibattito
      Done when: report HTML generato senza errori

- [ ] **TR-012** — Aggiunta `ornith-9b` (5.6 GB, fast) come opzione veloce
      Comando: edit `gui/app.py` MODELS + `_MODEL_FOR_PROVIDER` analogo ornith-35b
      Done when: `/api/models` mostra ornith-9b state=ok

- [ ] **TR-013** — Verifica path `bin/war-room-tavolarotonda` dopo mv
      Comando: `cat /home/mrxxx/Obsidian/Memoria/progetti/tavolarotonda-due/bin/war-room-tavolarotonda | head -5` + `war-room-tavolarotonda --mock --rounds 1 "test"`
      Done when: exit 0

## Failed Approaches

- **`pnpm dev` background senza CI=true** → `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY` (Manus, non questo progetto — ma pattern applicabile a tutti i workspace pnpm)
- **`pgrep -af "google-chrome.*localhost"` per verificare Chrome** → non funziona (Chrome è demone singolo); la conferma è `curl HTTP 200` + output "Apertura nella sessione del browser esistente"

## Do NOT

- NON usare `git mv` per spostare il repo (il repo NON era dentro un repo git padre — usare `mv` normale)
- NON nominare il progetto `tavolarotonda` (nome occupato dalla dir documentazione vault storica in `progetti/tavolarotonda/`)
- NON aggiungere `ollama-auto` al routing triade/alternating (sostituito da `ornith-35b` che è più specifico e testato)
- NON committare senza aver verificato che i 3 file modificati (`gui/app.py`, `providers.py`, `_INDEX.md`, `tavolarotonda-due.md`) siano tutti staged correttamente

## Key Decisions

- **Rename → `tavolarotonda-due`**: per distinguersi dalla dir documentazione concettuale esistente `progetti/tavolarotonda/`
- **Ornith sostituisce ollama-auto nel routing**: `ornith-35b:latest` è il modello Ollama di default attivo (occupa il top della lista), testato OK, 262k context. `ollama-auto` resta come opzione standalone ma fuori dai preset multi-provider.
- **GUI su `:8912`** (non 5000 come da README) — porta impostata in `gui/app.py __main__` argparse default

## Current State

- GUI: `http://localhost:8912/` HTTP 200 · task bg `b4i7pvl7m`
- Repo: `/home/mrxxx/Obsidian/Memoria/progetti/tavolarotonda-due/` · HEAD `da1f90b`
- Git status: `M gui/app.py` · `M tavolarotonda/providers.py` · `M tavolarotonda/graphify-out/.graphify_root` · `?? _INDEX.md` · `?? tavolarotonda-due.md`
- Modelli disponibili: mock ✅ · ollama-auto ✅ · opus-local ✅ · **ornith-35b ✅** · opus-4.8 ⚠️ (ANTHROPIC_API_KEY) · MiniMax-M3 ⚠️ (MiniMax_API_KEY)

## File Changes

| File | Tipo | Modifica |
|---|---|---|
| `gui/app.py` | modifica | +ornith-35b MODELS/routing/descrizione triade |
| `tavolarotonda/providers.py` | modifica | +`_THINK_BLOCK_RE` strip `<think>` |
| `tavolarotonda-due.md` | nuovo | entry vault canonica (untracked) |
| `_INDEX.md` | nuovo | auto-generato hook (untracked) |
| `.claude/PROJECT-TOD.md` | nuovo | PG-TOD bootstrap |
| `.claude/checkpoints/CP_20260702_0154.md` | nuovo | questo file |

## Code Context

```python
# gui/app.py — MODELS entry ornith-35b
"ornith-35b": {
    "label": "Ornith 35B (qwen3.6 MoE, ctx 256k)",
    "provider_kind": "ollama",
    "env_required": [],
    "default_model": "ornith-35b:latest",
    "icon": "🐦",
}
# _MODEL_FOR_PROVIDER
"ornith-35b": "ornith-35b:latest",

# providers.py — _strip_think fix
_THINK_BLOCK_RE = re.compile(r"<think>.*?</think>", re.DOTALL | re.IGNORECASE)
def _strip_think(text):
    t = _THINK_BLOCK_RE.sub("", text)
    t = _THINK_RE.sub("", t)  # Qwen3 unicode pattern
    ...
```

## Resume Instructions

1. Leggi questo checkpoint
2. Il PRIMO task è **TR-010** (commit + push) — eseguibile immediatamente
3. GUI riavviala con: `cd /home/mrxxx/Obsidian/Memoria/progetti/tavolarotonda-due && python gui/app.py`

## Warnings

- Remote GitHub `eroslifestyle/tavolarotonda` potrebbe essere disallineato dopo il `mv` locale — verifica con `git remote -v` e se necessario `git push` (potrebbe richiedere auth)
- Il file `.gitignore` potrebbe escludere `tavolarotonda-due.md` e `_INDEX.md` — controlla prima del commit
