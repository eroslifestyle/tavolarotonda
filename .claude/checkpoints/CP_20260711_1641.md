---
name: CP_20260711_1641
type: checkpoint
project: tavolarotonda-due
session: 2026-07-11
---

# CP_20260711_1641 — tavolarotonda-due TR-014

**HEAD**: `4b3f7c2` · **Branch**: main · **Sessione**: 2026-07-11

## Goal
TR-014 — Ornith-35b nella triade: valutare se sostituire anche in routing CLI default.

## Progress/TODO Board

✅ **Done** (evidence-gated):
- [x] **TR-014** — Ornith-35b in MODEL_TIER_MAP + --model CLI flag + tier resolution wiring · `4b3f7c2` (2026-07-11)
      Evidence: `MODEL_TIER_MAP['ornith']='ornith-35b'` · `--help` show `--model` + `ornith` in intensity · mypy 0 err · 30/30 tests PASS · push OK

⬜ **To-Do** (in ordine):
- [ ] **TR-030** — Claude API SDK (ora solo HTTP raw) — roadmap v0.2
      Comando: `cd "/mnt/backup/Dropbox/1 Programmazione/Progetti/tavolarotonda-due" && grep -r "httpx\|requests" tavolarotonda/`
      Done when: Claude SDK sostituisce HTTP raw, test PASS
- [ ] **TR-031** — SearXNG/Brave search (mock fallback OK per ora)
      Comando: `cd "/mnt/backup/Dropbox/1 Programmazione/Progetti/tavolarotonda-due" && grep -r "search\|brave\|searxng" tavolarotonda/`
      Done when: search provider implementato con fallback graceful
- [ ] **TR-032** — Pubblicazione repo pubblico GitHub — decisione utente

## Failed Approaches

None in questa sessione.

## Do NOT

Non toccare `.env`/secrets · Non riscrivere la GUI · Non aggiungere auth/multi-user

## Key Decisions

- Ornith-35b aggiunto come tier `"ornith"` in MODEL_TIER_MAP — raggiungibile via `--model ornith-35b` OPPURE `--intensity ornith`
- Routing: `_resolve_agent_model()` helper in `phases.py` risolve `default_model="auto"` → `council_model` con tier mapping
- `council_model` wired in `run_full_council` → `phase_brainstorm` + `phase_critique` → `provider.complete(model=, model_tier=)`

## Current State

tavolarotonda-due funziona con CI attivo (30/30 PASS, mypy 0), MCP server + Obsidian vault HTTP attivi. Ornith-35b pienamente integrato nel routing CLI. Tutti i test passano, pushato.

## File Changes

- `tavolarotonda/providers.py` — MODEL_TIER_MAP: aggiunto `"ornith": "ornith-35b"`
- `tavolarotonda/__main__.py` — aggiunto `--model` flag + `"ornith"` in `--intensity` choices + `council_model` wiring in 3 funzioni
- `tavolarotonda/phases.py` — `_resolve_agent_model()` helper + `council_model` param in `run_full_council/phase_brainstorm/phase_critique` + model_tier wiring

## Code Context

```python
# providers.py
MODEL_TIER_MAP: dict[str, str] = {
    "critical": "opus-4-7",
    "reasoning": "minimax-sonar-pro",
    "standard": "claude-sonnet-5",
    "fast": "claude-haiku-4",
    "ornith": "ornith-35b",
}

# __main__.py (run_topic/run_audit/run_qa)
await run_full_council(..., council_model=getattr(args, "model", None), ...)

# phases.py
def _resolve_agent_model(agent_default: str, council_model: str | None) -> tuple[str, str | None]:
    if agent_default != "auto":
        return agent_default, None
    if council_model:
        tier_map = {"ornith-35b": "ornith", "ollama/ornith-35b": "ornith"}
        return council_model, tier_map.get(council_model)
    return "mock", None
```

## Setup

Standard CLI: `python -m tavolarotonda "topic" [--model ornith-35b] [--intensity ornith]`
MCP server: `python -m tavolarotonda serve --port 8765`
Obsidian vault: `python -m tavolarotonda serve` (API attiva)

## Warnings

None.

## Bootstrap prossima chat

```
Leggi il checkpoint: /mnt/backup/Dropbox/1 Programmazione/Progetti/tavolarotonda-due/.claude/checkpoints/CP_20260711_1641.md
Task corrente: TR-030 — Claude API SDK (sostituire HTTP raw con SDK)
Non toccare: .env/secrets · GUI · auth/multi-user
Quando hai finito, committa con messaggio conventional, pusha, e fai /wiki all.
```
