---
name: CP-20260711-1008
description: "TR-044/045/020/021 completati: mypy 0 errori, Obsidian vault, MCP server, opus-m3-confer wiring"
---

# Checkpoint CP_20260711_1008

**Goal**: completa TR-045 + TR-020 + TR-021 in unica sessione
**Stato**: ✅ **TUTTI COMPLETATI** · HEAD `db5b90c` · main

---

## ✅ Done (evidence-gated)

- **TR-044 mypy fix**: 12 errori → 0. Files: `providers.py` (`x-api-key | None`), `evidence.py` (`urllib.parse` import), `phases.py` (`res.error` union narrow, `events` type annotation), `__main__.py` (`PhaseEvent` import, 3× `events: list[PhaseEvent]`). Commit `db5b90c`.
- **TR-045 Obsidian vault**: `obsidian_vault.py` + `serve.py` + test + deps. `GET /api/obsidian/topic?topic=X`, `POST /api/obsidian/save`, `GET /health`. Commit `db5b90c`.
- **TR-020 MCP server**: `mcp_server.py` (discuss/status/history tools) + `POST /mcp` JSON-RPC 2.0 + 6 test PASS. Commit `db5b90c`.
- **TR-021 opus-m3-confer**: `MODEL_TIER_MAP` + `confer_phase()` + `--intensity` CLI flag + `MemoryPalace.confer_output`. Commit `db5b90c`.
- **Validazione**: ruff 0 errors · mypy 0 issues · pytest **30/30 PASS**
- **Git push**: `db5b90c` → origin/main

---

## ✅ Done recenti (contexte precedente)

- **TR-044** — GitHub Actions CI: workflow test+lint+typecheck, pyproject.toml, ruff fixes 14 file · `fa2db97`
- **TR-046/042/047** — Unificato provider + AQ scores + color stream · `49e5619`

---

## ⬜ To-Do (nessuno — tutti completati)

Nessun task residuo. Prossimi in PG-TOD:

- **TR-014** — Ornith-35b nella triade (valutare se sostituire in routing CLI default)
- **TR-030** — Claude API SDK (ora solo HTTP raw) — roadmap v0.2
- **TR-031** — SearXNG/Brave search (mock fallback OK per ora)

---

## ❌ Failed

Nessuno.

---

## Do NOT

- Non toccare `.env`/secrets
- Non riscrivere la GUI
- Non aggiungere auth/multi-user

---

## Key Decisions

1. **MCP senza dipendenze esterne**: protocollo JSON-RPC 2.0 su HTTP POST, implementato in ~200 righe Python
2. **serve.py unificato**: HTTP API (Obsidian + MCP) in un solo server Starlette, importato dal CLI "serve"
3. **MODEL_TIER_MAP minimal**: 4 tier (critical/reasoning/standard/fast) senza routing automatico complesso
4. **mypy fix pattern**: `# type: ignore[union-attr]` per union narrow dopo isinstance check non riconosciuto

---

## Current State

- **CI attivo**: GitHub Actions workflow test+lint+typecheck su push/PR
- **7 file nuovi**: `obsidian_vault.py`, `serve.py`, `mcp_server.py` + 4 test files
- **pyproject.toml**: `starlette>=0.27`, `uvicorn>=0.27`, `httpx>=0.25` aggiunte
- **CLI**: `python -m tavolarotonda serve [--port] [--vault]` ora funziona
- **PG-TOD aggiornato**: TR-045/020/021 spostati da ⬜ Backlog → ✅ Done

---

## File Changes (commit db5b90c)

| File | Delta |
|---|---|
| `tavolarotonda/obsidian_vault.py` | nuovo |
| `tavolarotonda/serve.py` | nuovo |
| `tavolarotonda/mcp_server.py` | nuovo |
| `tavolarotonda/providers.py` | +MODEL_TIER_MAP, +model_tier param |
| `tavolarotonda/phases.py` | +confer_phase, +intensity param |
| `tavolarotonda/memory_palace.py` | +confer_output field |
| `tavolarotonda/__main__.py` | +serve subcmd, +--intensity flag |
| `tavolarotonda/__init__.py` | +read_topic, save_session exports |
| `tavolarotonda/evidence.py` | +urllib.parse import |
| `pyproject.toml` | +starlette/uvicorn/httpx |
| `tests/test_*.py` | 4 nuovi file, 16 nuovi test |

---

## Setup

```bash
cd "/mnt/backup/Dropbox/1 Programmazione/Progetti/tavolarotonda-due"
pip install starlette uvicorn httpx -q
python -m pytest tests/ -q  # → 30/30 PASS
python -m mypy tavolarotonda/ --ignore-missing-imports  # → 0 issues
```

---

## Resume Instructions

Questa sessione ha chiuso tutti i task aperti. Per continuare:
1. Leggi `.claude/PROJECT-TOD.md` per vedere stato attuale del progetto
2. Prossimi task: TR-014, TR-030, TR-031 (backlog del PG-TOD)
3. Prima di ogni sessione: leggi PG-TOD + checkpoint per orientamento

---

## Warnings

- Nessuno.

---

**PG-TOD delta**: 3 items (TR-044/045/020/021) spostati da ⬜ Backlog → ✅ Done
**Sessioni vault**: `progetti/tavolarotonda/sessioni/2026-07-11-tr-045-020-021.md`
