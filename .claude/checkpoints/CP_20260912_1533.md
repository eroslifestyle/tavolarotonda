# CP_20260912_1533

## Goal
Far dialogare Claude Code e Codex CLI nello stesso council (tavolarotonda-due) usando i login OAuth degli abbonamenti (Claude Max, ChatGPT Plus/Pro) invece di API key a consumo.

## Progress/TODO Board
- [x] `codex` binario trovato dentro estensione VSCode "ChatGPT" (`~/.vscode/extensions/openai.chatgpt-26.908.40401-linux-x64/bin/linux-x86_64/codex`, 0.154.0-alpha.6.2); `~/.codex/auth.json` già OAuth autenticato
- [x] Symlink `~/.local/bin/codex` creato; `codex --version` OK
- [x] Formati output verificati: `claude -p ... --output-format json` → campo `"result"`; `codex exec ... -s read-only --skip-git-repo-check -o <file>` → testo puro su file
- [x] Provider implementati e agganciati: `tavolarotonda/providers.py` (kind `claude_cli`/`codex_cli`, metodi `_claude_cli`/`_codex_cli`, classi `ClaudeCliProvider`/`CodexCliProvider`), `tavolarotonda/config.yaml` (voci `claude-cli-oauth`, `codex-cli-oauth`, `env_required: []`), `tavolarotonda/debate.py` `_make_provider`, `gui/app.py` `_build_provider` + `/api/models`
- [x] Smoke test reale: `complete(..., provider_kind="claude_cli")` → `'PONG'`; stesso per `codex_cli`
- [x] Commit `d227434` "feat: provider CLI OAuth (claude_cli, codex_cli)" (10 file, main, NON pushato)
- [ ] **PROSSIMO STEP**: eseguire un vero turno di roundtable con `claude-cli-oauth` + `codex-cli-oauth` su topic reale (es. CRAM Plugin) nel flow debate/GUI — expected outcome: trascrizione dibattito con risposte reali di entrambe le CLI, non mock

## Failed Approaches
None (nessun vero fallimento; una sola strada scartata a monte, non provata: npm install standalone di codex — avrebbe richiesto rifare login e il nome pacchetto non era verificabile da tracce filesystem locali)

## Do NOT
- NON reinstallare codex via npm senza conferma esplicita dell'utente
- NON modificare `tavolarotonda/graphify-out/` a mano (si rigenera da solo via hook post-commit; 5 file modificati non committati = normale, fuori scope)
- NON pushare il commit `d227434` senza richiesta esplicita
- NON toccare i provider_kind esistenti (ollama/openai_compat/claude/anthropic_compat) — solo aggiunta
- I provider CLI nel council: permission-mode "plan" (claude) e sandbox "read-only" (codex) — NESSUNA scrittura su file durante il dibattito

## Key Decisions
- Symlink invece di npm install: zero setup, riusa OAuth già attivo; tradeoff accettato = build alpha pinnata all'estensione VSCode (si rompe se disinstallata/aggiornata incompatibilmente)
- permission-mode "plan" / sandbox "read-only": i due provider CLI devono solo discutere, mai editare
- Sottoclassi dedicate (`ClaudeCliProvider`/`CodexCliProvider`) invece di passare `provider_kind` a mano: i call-site reali non lo passano esplicitamente; segue il pattern esistente `AnthropicCompatProvider`, zero architettura nuova

## Resume Instructions
Repo: `/mnt/nvme2/projects/Progetti/tavolarotonda-due`, HEAD `d227434`, branch main, working tree pulito eccetto `graphify-out/` (auto-generato). Per il prossimo step: lanciare un dibattito (CLI `python -m tavolarotonda` o GUI `python gui/app.py` su :5000) con config provider `claude-cli-oauth` + `codex-cli-oauth` su un topic reale; verificare che entrambi i provider rispondano nel flusso completo (Research→Restate→Brainstorm→Critique→Synthesis→Vote). Se il symlink `codex` è rotto (estensione VSCode aggiornata), ri-puntarlo al nuovo path dentro `~/.vscode/extensions/openai.chatgpt-*/bin/linux-x86_64/codex`.
