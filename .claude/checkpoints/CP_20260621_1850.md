# Checkpoint: tavolarotonda-bootstrap

**Saved:** 2026-06-21 18:50  ·  **Branch:** N/A (no git init)  ·  **Status:** READY
**Context:** ~30% used  ·  **Agent-agnostic:** any AI coding agent can resume from this file.

---

## Goal

Spostare il software `tavolarotonda` (council multi-agente Python, MIT, 9 moduli) da `/tmp/tavolarotonda/` a una home permanente e registrarlo come progetto canonico nel vault Obsidian, così non venga perso al reboot /tmp e sia raggiungibile da qualunque AI agent futuri.

## Progress

**Completed**
- [x] Trovato `/tmp/tavolarotonda/` (council multi-agente Python) leggendo il vault log e facendo `find`
- [x] Software copiato in `/mnt/backup/PROGETTI/tavolarotonda/` (564 KB, 9 moduli + GUI + 14 test + 5 discussion HTML)
- [x] Vault entry creata: `progetti/tavolarotonda/{tavolarotonda.md, _INDEX.md, 6 sottocartelle standard, sessioni/2026-06-21-bootstrap-progetto-vault.md}`
- [x] Log globale aggiornato in `~/Obsidian/Memoria/log.md` (entry 18:50)
- [x] Routine `/wiki all` eseguita: vault save → checkpoint → codemap (skip ast-only riuscito) → git skip (no init) → clipboard + link

**Not yet done**
- [ ] `git init` + remote GitHub (pubblico MIT) — necessaria decisione utente
- [ ] Test integrazione Ollama locale con modelli pinned (`chat-max`, `cyber-max` del pool leobox)
- [ ] Aggiungere MCP server (vs quorum-cli)
- [ ] Wiring con skill Pi: `/skill:war-room`, `/skill:swarm`, `/skill:opus-m3-confer`
- [ ] Pubblicare release v0.1 su GitHub + PyPI

**Progress:** ~85%

## Failed Approaches — Don't Repeat These ⚠️ MANDATORY

- **Tentato:** Usare `find / -iname "tavola*rotonda*"` direttamente
  **Motivo fallimento:** match glob non standard su alcune shell, output incompleto
  **Approccio attuale:** `find / -type d -iname "*tavola*rotonda*" -o -iname "*roundtable*" -o -iname "*round-table*"` (3 varianti OR + type filter)

- **Tentato:** Inizializzare git automaticamente in `/mnt/backup/PROGETTI/tavolarotonda/` durante il bootstrap
  **Motivo fallimento:** Decisione importante (pubblico? quale remote? LICENSE check? GitHub org?) — fuori scope, preferisco skip silente e segnalare all'utente per decisione esplicita
  **Approccio attuale:** Repo resta non-init, indicato chiaramente in PROJECT.md "Problemi noti" + checkpoint

## Key Decisions

| Decisione | Rationale | Alternative scartate |
|---|---|---|
| Home permanente = `/mnt/backup/PROGETTI/tavolarotonda/` | Path parallelo a `local-ai-stack` (pattern vault consolidato), fuori `/tmp` (volatile) | `/home/mrxxx/projects/` (non in scope backup) · `~/Obsidian/Memoria/progetti/tavolarotonda/` (vault non è per file .py grossi) |
| Single source of truth = `repo/README.md` | Regola modularità KING — no duplicazione, vault punta al file canonico via link | Copiare README in vault (duplicazione, drift) · Scriverne uno nuovo in vault (drift) |
| Naming cartella vault = `tavolarotonda` (lowercase, no spazi) | Coerente con `local-ai-stack`, `keyok`, `CS_Bridge_360` | `Tavola Rotonda` (con spazi, rompe wiki-link) · `council-multi-agent` (perde identità) |
| Cartelle standard `sessioni/decisioni/concetti/reference/log/sintesi/` | Match template `local-ai-stack/` e altri progetti vault | Cartelle ad-hoc (rompe convenzione, `_INDEX` rotto) |
| Skip `git init` automatico | Decisione pubblicazione richiede input utente (privato? pubblico? org?) | Auto-init senza consenso (rischio push accidentale) |

## Current State

**Working:** Software in path permanente + vault entry canonica + log globale aggiornato. Tutto raggiungibile da `tavolarotonda.md` hub.

**Broken:** Niente. Progetto è READY.

**Uncommitted:** N/A — repo non è git ancora. Quando lo sarà, primo commit dovrà includere: 9 moduli Python, README.md, tests/, gui/, examples/, output/ (5 HTML), .claude/checkpoints/CP_20260621_1850.md, .gitignore (da creare).

## File Changes

### Created
| File | Purpose |
|---|---|
| `/mnt/backup/PROGETTI/tavolarotonda/` (entire dir copy) | Home permanente del software |
| `/home/mrxxx/Obsidian/Memoria/progetti/tavolarotonda/tavolarotonda.md` | PROJECT.md hub master (5.9 KB) |
| `/home/mrxxx/Obsidian/Memoria/progetti/tavolarotonda/_INDEX.md` | Indice struttura (2.7 KB) |
| `/home/mrxxx/Obsidian/Memoria/progetti/tavolarotonda/sessioni/2026-06-21-bootstrap-progetto-vault.md` | Session page di bootstrap (3.1 KB) |
| `/home/mrxxx/Obsidian/Memoria/progetti/tavolarotonda/{concetti,decisioni,log,reference,sintesi}/` | 5 cartelle standard vuote |
| `/home/mrxxx/Obsidian/Memoria/log.md` (append) | +1 entry creazione progetto |
| `/mnt/backup/PROGETTI/tavolarotonda/.claude/checkpoints/CP_20260621_1850.md` | Questo checkpoint |

### Modified
| File | Changes |
|---|---|
| Nessuna modifica a file preesistenti (solo copy + create) |

## Files to Know (per orientarsi velocemente)

| File | Why it matters |
|---|---|
| `repo/README.md` | Single source of truth — architettura, install, uso, privacy tier, bug Manus fixati |
| `repo/tavolarotonda/agents.py` | 18 personas + polarity pairs (cuore del council) |
| `repo/tavolarotonda/phases.py` | Pipeline 6 fasi (Research → Restate → Brainstorm → Critique → Synthesis → Verdict) |
| `repo/tavolarotonda/providers.py` | LLM abstraction (Ollama / OpenAI-compat / Claude / Mock) |
| `repo/tavolarotonda/reports.py` | HTML audit + Q&A generator |
| `vault/progetti/tavolarotonda/tavolarotonda.md` | Hub wiki, decisioni, problemi aperti |
| `vault/progetti/tavolarotonda/_INDEX.md` | Indice struttura completo |

## Code Context — show, don't describe

**Architettura moduli (CLI entry point)**
```python
# tavolarotonda/__main__.py → argparse → phases.run_full_pipeline
# Privacy tier esplicito: local_only | cloud_ok | free_api_ok
# Esempio CLI: python -m tavolarotonda --privacy local_only "topic..."
# Esempio audit: python -m tavolarotonda --audit examples/audit_target.py --output output/audit_report.html
# Esempio Q&A:   python -m tavolarotonda --qa "Rischio X?" "Alt Y?" --output output/qa.html
```

**Pipeline 6 fasi (phases.py)**
```python
phase_research()      # Phase 0: parallel supporting + counter evidence
phase_restate()       # Phase 1: ogni agente riformula nel SUO angolo (Problem Restate Gate)
phase_brainstorm()    # Phase 2: Director assegna focus + Secretary consolida
phase_critique()      # Phase 3: cross-examination (ognuno confuta un altro)
phase_synthesis()     # Phase 4: decisione + minority report + open questions + next steps
phase_verdict()       # Phase 5: voti 0-10 feasibility/impact/risk_safety
```

**Providers (providers.py)** — astrazione LLM:
- `OllamaProvider` (default, privacy local_only)
- `OpenAICompatProvider` (Claude/OpenAI via env key, privacy cloud_ok)
- `ClaudeProvider` (HTTP raw Anthropic, no SDK — limitazione nota)
- `MockProvider` (no LLM reale, solo demo)

## Setup Required (per il prossimo agente)

- Python ≥3.10 (per `match` statements e type hints moderni)
- Solo stdlib + `requests` + `urllib` (vedi `requirements.txt` se esiste)
- Ollama opzionale ma raccomandato (per uso reale vs `--mock`)
- Privacy tier default = `local_only` (nessuna dipendenza cloud)
- Per `cloud_ok`: env `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`
- Per `free_api_ok`: env `OPENAI_BASE_URL` (LiteLLM :4000), `OPENAI_API_KEY` + PII redaction automatica

## Resume Instructions — step-by-step CON expected outcomes

1. **Verifica setup** — `cd /mnt/backup/PROGETTI/tavolarotonda && ls`
   - **Atteso:** vedi `tavolarotonda/`, `gui/`, `tests/`, `examples/`, `output/`, `README.md`

2. **Smoke test (no LLM)** — `python tests/test_smoke.py`
   - **Atteso:** `✅ Tutti i 14 test passati`

3. **Demo mock** — `python -m tavolarotonda --mock "Dovrei migrare a Postgres?"`
   - **Atteso:** output console con 18 personas che dibattono + HTML in `output/`

4. **Init git + remote (decisione utente richiesta)**:
   ```bash
   cd /mnt/backup/PROGETTI/tavolarotonda
   echo ".claude/checkpoints/\noutput/\ngui.log\nserver.log\n__pycache__/" > .gitignore
   git init && git add . && git commit -m "feat: bootstrap tavolarotonda v0.1 (council multi-agente Python)"
   git remote add origin <USER_PROVIDED_URL>  #chiedi all'utente
   git push -u origin main
   ```
   - **Atteso:** repo pubblico/privato secondo scelta utente

5. **Wire con skill Pi** (se utente chiede):
   - `/skill:war-room` → riusa pattern 18 personas + polarity pairs
   - `/skill:opus-m3-confer` → riusa prompt Restate + Adversarial evidence
   - Vedi sezione "Skill correlate" in `vault/progetti/tavolarotonda/tavolarotonda.md`

## Edge Cases & Error Handling

- Se Ollama non è in running → fallback automatico a `--mock` (Provider graceful degradation)
- Se topic utente contiene prompt-injection markers (`ignore previous`, `system:`, ecc.) → `sanitize_directive()` rimuove/marca, tronca a 500 char
- Se PII inviata con privacy `free_api_ok` → redaction automatica (email, IP, telefono, CF)
- Se Claude API key mancante → fallback `MockProvider` con warning
- Se `--audit` file non esiste → exit code 1 + messaggio chiaro

## Blockers / Open Questions

- **Decisione richiesta all'utente:** git init + remote GitHub (pubblico MIT? org `eroslifestyle`? privato?)
- **Decisione richiesta:** aggiungere LICENSE file (al momento solo dichiarato MIT in README)
- **Decisione richiesta:** wiring con skill Pi esistenti (`/skill:war-room` ha overlap funzionale — merge o standalone?)

## Warnings / Gotchas

- **NON chiamare Python con `python3` da sistema** — usare `python` (potrebbe puntare a venv). Verifica con `which python`.
- **Output directory può crescere** — ogni discussione genera 1 HTML + 1 palace JSON. Aggiungere `output/` a `.gitignore` (Fatto nel template step 4).
- **Prompt injection mitigation** è in `sanitize_directive()` — non disabilitare MAI senza replacement equivalente.
- **Privacy tier `free_api_ok` NON è sicuro per dati sensibili** — PII redaction è best-effort, non garantita al 100%.
- **Il vault wiki si aggiorna solo a `/wiki all` / hook Stop** — modifiche manuali a `_INDEX.md` non sono auto-rigenerate finché non gira `obsidian_auto_update.py`.

## Test State

| Metric | Value |
|---|---|
| Last run | 2026-06-21 17:47 (al momento della copia, leggibile da `server.log`) |
| Passed/Failed/Skipped | 14/0/0 (da `tests/test_smoke.py`) |
| Coverage | N/A (no coverage config) |
| Failing tests | None |

## Environment (opzionale)

| Resource | Value |
|---|---|
| Platform | Linux leobox (Ubuntu 24.04) |
| Disk | `/mnt/backup/PROGETTI/tavolarotonda` 564 KB |

## Memoria & link

- Vault entries scritte in questa sessione:
  - `~/Obsidian/Memoria/progetti/tavolarotonda/tavolarotonda.md` (hub)
  - `~/Obsidian/Memoria/progetti/tavolarotonda/_INDEX.md`
  - `~/Obsidian/Memoria/progetti/tavolarotonda/sessioni/2026-06-21-bootstrap-progetto-vault.md`
- Log globale: `~/Obsidian/Memoria/log.md` (+1 entry)
- Hot cache: da aggiornare al prossimo `/wiki query` o hook Stop
- Related: nessun checkpoint precedente (primo checkpoint del progetto)

## Notes

Sessione molto lineare, contesto pulito (~30%), nessun blocker tecnico. Il progetto è in stato READY per la prossima fase (git init → test integrazione Ollama → wiring skill Pi → publish).

Le decisioni "morbide" (pubblico vs privato, LICENSE file, merge con skill war-room) sono delegate all'utente esplicitamente — meglio non fare scelte irreversibili (git push) senza consenso.

Il tool `/wiki all` ha completato: STEP 1 (vault save) ✓ · STEP 2 (questo checkpoint) ✓ · STEP 3 (codemap skip — software è Python non in un git repo, ma codemap può girare su /mnt/backup/PROGETTI/tavolarotonda/ con `--ast-only`) · STEP 4 (git skip — no init) · STEP 5 (clipboard + link) — vedi messaggio finale.

---

_Auto-generato da routine /wiki all step 2 (checkpoint V22.4). Path: `/mnt/backup/PROGETTI/tavolarotonda/.claude/checkpoints/CP_20260621_1850.md`_