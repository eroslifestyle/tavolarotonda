# Checkpoint CP_20260711_2350

**Project**: tavolarotonda-due
**Session**: 2026-07-11 TR-044 CI
**HEAD**: fa2db97 (feat(TR-044): add GitHub Actions CI)

---

## ✅ Done

- **TR-044** — GitHub Actions CI: `.github/workflows/ci.yml` (test/lint/typecheck) + `pyproject.toml` + ruff fixes (14 file) · `fa2db97`
  - Test: pytest 3.11/3.12 + coverage codecov
  - Lint: ruff 0 errors
  - Typecheck: mypy (8 errors tracked separately)
- **PG-TOD updated**: TR-044 spostato in ✅ Done, ⬜ Backlog aggiornato

---

## Progress / TODO Board

### ✅ Completed this session
- [x] **TR-044** — GitHub Actions CI
      Done: workflow `.github/workflows/ci.yml` attivo su push/PR, ruff clean 0 errors, 14/14 test pass

### ⬜ To-Do (next session)

- [ ] **TR-045** — Integrazione Obsidian (topic da vault, risultati in vault)
      Comando: `cd "/mnt/backup/Dropbox/1 Programmazione/Progetti/tavolarotonda-due" && python -m tavolarotonda serve`
      Done when: endpoint `/api/obsidian-topic` risponde, output markdown salvato in vault

- [ ] **TR-020** — MCP server (tavolarotonda serve --mcp)
      Comando: `cd "/mnt/backup/Dropbox/1 Programmazione/Progetti/tavolarotonda-due" && python -m tavolarotonda serve --mcp`
      Done when: client test OK, risposta HTTP 200 da MCP handshake

- [ ] **TR-021** — Wiring con `opus-m3-confer` per routing decisioni complesse
      Riferimento: vault `progetti/tavolarotonda-due/`

- [ ] **mypy 8 errors** — Fix type annotation mancanti
      File: `tavolarotonda/providers.py:279`, `tavolarotonda/phases.py`, `tavolarotonda/__main__.py`
      Done when: `mypy tavolarotonda/ --ignore-missing-imports` exit 0

---

## ❌ Failed Approaches

None in this session.

---

## Do NOT

- Non committare `.env` / secrets
- Non riscrivere la GUI
- Non aggiungere auth/multi-user

---

## Key Decisions

- Ruff per-file ignore E402 per `tests/*` (path manipulation necessario per sys.path.insert)
- mypy 8 errori non bloccanti nel CI (type annotation deferred)
- pyproject.toml creato per centralizzare config packaging + tool

---

## Current State

CI attivo su GitHub Actions. Prossimi passi: TR-045 (Obsidian), TR-020 (MCP), fix mypy.

---

## File Changes

**Created:**
- `.github/workflows/ci.yml` — GitHub Actions CI
- `pyproject.toml` — packaging + tool config

**Modified (ruff fix):**
- `tavolarotonda/__init__.py` — import sorting
- `tavolarotonda/__main__.py` — import sorting, unused `ext` removed
- `tavolarotonda/agents.py` — unused `field` removed
- `tavolarotonda/config.py` — `stacklevel=2` warnings.warn
- `tavolarotonda/director.py` — import sorting, unused `Iterable` removed
- `tavolarotonda/evidence.py` — import sorting, unused `urllib.parse` removed
- `tavolarotonda/memory_palace.py` — import sorting
- `tavolarotonda/phases.py` — import sorting, unused `agent_names` removed, zip strict
- `tavolarotonda/providers.py` — import sorting
- `tavolarotonda/reports.py` — import sorting
- `tavolarotonda/secretary.py` — import sorting
- `tests/test_smoke.py` — import sorting
- `.claude/PROJECT-TOD.md` — TR-044 in ✅ Done

**Commit:** `fa2db97` on `main`

---

## Code Context

```python
# CI workflow key jobs:
# test: pytest -v --cov=tavolarotonda --cov-report=xml
# lint: ruff check tavolarotonda/ tests/
# typecheck: mypy tavolarotonda/ --ignore-missing-imports
```

---

## Setup

```bash
cd "/mnt/backup/Dropbox/1 Programmazione/Progetti/tavolarotonda-due"
git pull  # sync HEAD
python -m pytest tests/ -q  # verify 14/14 pass
ruff check tavolarotonda/ tests/  # verify 0 errors
```

---

## Warnings

- mypy ha 8 errori reali (type annotation mancanti) — non bloccano CI ma vanno fixati
- 2 nuovi file non tracciati: `output/discussion_*.html`, `sessioni/*.md` — non committati

---

## Bootstrap prossima chat — copia-incolla prompt iniziale

```
Leggi il checkpoint: /mnt/backup/Dropbox/1 Programmazione/Progetti/tavolarotonda-due/.claude/checkpoints/CP_20260711_2350.md
Task corrente: TR-045 — Integrazione Obsidian (topic da vault, risultati in vault)
Non toccare: .env/secrets, GUI, auth/multi-user
Quando hai finito, committa con messaggio conventional, pusha, e fai /wiki all.
```
