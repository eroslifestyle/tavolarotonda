---
updated: 2026-07-12T06:29
tags: [checkpoint, tavolarotonda]
type: checkpoint
---

# CP_20260712_0629 — tavolarotonda-due

**HEAD**: `b880ce4` · **Branch**: `main`

## Goal
Fix e integrazione API MiniMax + GLM in tavolarotonda.

## Progress/TODO Board

✅ **Done verificati**:
- [x] MiniMax endpoint: `https://api.minimax.io/anthropic` + `X-Api-Key` header · `2c5b86b`
- [x] GLM endpoint: `https://api.z.ai/api/anthropic` + `Authorization: Bearer` · `74039eb`
- [x] providers.py: headers multipli per compatibilità trasversale · `b880ce4`
- [x] GUI: `api_models` gestisce `anthropic_compat` (MiniMax, GLM) · `b880ce4`
- [x] Desktop entry: carica secrets prima di avviare · pushato
- [x] GLM aq_score: 4.7=7.0, 5.2=7.5 · `b880ce4`
- [x] 30/30 test PASS

⬜ **To-Do**: Nessuno — tutti i task completi.

## Failed Approaches

- `sk-api-...` per MiniMax → errore 2049 (formato chiave sbagliato) · risolto con `sk-cp-...`
- `bigmodel.cn` per GLM → errore 1113 credito 0 · risolto con `z.ai`
- `minimaxi.com` per MiniMax → "invalid api key" · risolto con `minimax.io`
- `api.z.ai/api/anthropic` (senza `/v1/messages`) → 404

## Do NOT

Non toccare .env/secrets · Non riscrivere la GUI strutturalmente

## Key Decisions

- MiniMax: direct endpoint `api.minimax.io/anthropic`, non ai-router
- GLM: z.ai endpoint Anthropic-compat, non bigmodel.cn
- Headers multipli in `_anthropic_compat` (`Authorization` + `x-api-key` + `X-Api-Key`) per compatibilità
- GLM-4.6 rimosso (non disponibile su z.ai), aggiunto GLM-5.2

## Current State

tavolarotonda-due HEAD `b880ce4` — MiniMax e GLM integrati e funzionanti via providers.py.

## File Changes

- `tavolarotonda/config.yaml` — GLM provider_kind=anthropic_compat, endpoint z.ai, aq_score
- `tavolarotonda/providers.py` — X-Api-Key + Authorization headers in _anthropic_compat
- `gui/app.py` — anthropic_compat in api_models
- `~/.local/share/applications/ai-round-table.desktop` — carica secrets

## Setup

```bash
cd "/mnt/backup/Dropbox/1 Programmazione/Progetti/tavolarotonda-due"
source ~/.secrets/minimax.env 2>/dev/null
source ~/.secrets/glm.env 2>/dev/null
python3 -m pytest tests/ -q  # 30/30 PASS
```

## Resume Instructions

Nessun TR aperto. Progetto completo.
